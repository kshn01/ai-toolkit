---
title: Tailwind conventions
globs: ["**/*.tsx", "**/*.jsx"]
---
Use Tailwind utility classes. Avoid inline `style` props and one-off CSS files. Extract
repeated class strings into small components rather than `@apply`.
