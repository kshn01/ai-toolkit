---
title: TypeScript conventions
globs: ["**/*.ts", "**/*.tsx"]
---
Enable strict mode. Prefer `type` aliases and discriminated unions over enums. No `any` —
use `unknown` and narrow. Keep public function signatures explicitly typed.
